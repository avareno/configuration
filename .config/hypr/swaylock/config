# Use a background color (hex format)
--color 282828

# Show an image as the lock screen background
--image /path/to/image.png

# Display an indicator
--indicator

# Hide the mouse cursor when locked
--hide-cursor
